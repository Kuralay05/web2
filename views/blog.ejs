<%- include('layout', { title: blog.title, body: ` 
  <h1><%= blog.title %></h1>
  <p><%= blog.body %></p>
  <p><strong>Author:</strong> <%= blog.author.username %></p>
  <p><strong>Created at:</strong> <%= new Date(blog.createdAt).toLocaleString() %></p>
` }) %>
